# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_DATABASE=weddingcut

# AWS Cognito
COGNITO_REGION=eu-north-1
COGNITO_USER_POOL_ID=eu-north-1_zEPFcP8zc

# CORS
CORS_FRONTEND=http://localhost:5173

# Auth mode: "local" (JWT validation in-process) or "apigw" (API Gateway claims)
AUTH_MODE=local

# Server (solo dev locale)
PORT=3005
